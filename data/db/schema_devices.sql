CREATE TABLE public.ruijie_devices (
    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
    sn TEXT UNIQUE NOT NULL,
    mac_address TEXT,
    alias TEXT,
    ip_address TEXT,
    status TEXT,
    connection_type TEXT,
    clients INTEGER DEFAULT 0,
    last_online TEXT,
    last_log_history TEXT,
    radio1_power INTEGER,
    radio2_power INTEGER,
    offline_reason TEXT,
    group_id BIGINT,
    radio1_channel_util INTEGER,
    radio2_channel_util INTEGER,
    remark TEXT,
    hardware_version TEXT,
    product_type TEXT,
    common_type TEXT,
    product_class TEXT,
    radio1_channel INTEGER,
    radio2_channel INTEGER,
    group_name TEXT,
    dev_mode TEXT,
    sta_nums INTEGER,
    cpe_ip TEXT,
    recommend_software_version TEXT,
    software_version TEXT,
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
